import 'package:flutter/material.dart';

class ProjectTableWeb extends StatelessWidget {
  const ProjectTableWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
