import 'package:flutter/material.dart';

class ProjectTableMobile extends StatelessWidget {
  const ProjectTableMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
