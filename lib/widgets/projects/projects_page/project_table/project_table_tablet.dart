import 'package:flutter/material.dart';

class ProjectTableTablet extends StatelessWidget {
  const ProjectTableTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
